{
  "name": "ms-authentication",
  "version": "1.0.0",
  "description": "Microsserviço de Autenticação criado no Bootcamp DIO",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./",
    "build": "tsc -p .",
    "dev": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/index.ts"
  },
  "author": "Rodrigo Alexandre - narradorww",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.10.3",
    "typescript": "^4.4.3"
  }
}
